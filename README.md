1. issue1 запускается через терминал командой python -m pytest --doctest-modules -o doctest_optionflags=NORMALIZE_WHITESPACE issue1.py
2. issue2 запускается через терминал командой python -m pytest issue2.py
